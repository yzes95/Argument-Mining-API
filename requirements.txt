# Docker Requirements File for Argument Mining API
#
# This file lists the EXACT Python library versions from the training environment
# to ensure 100% reproducibility inside the Docker container.
# Using pinned versions prevents inconsistencies between training and deployment.
#
# To install, use:
# pip install -r requirements.txt

# --- GPU Library Installation (Must be first) ---
# Add the PyTorch and NVIDIA indexes to the search path. This is crucial for
# finding the special GPU-enabled packages like PyTorch and cuML.
--extra-index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.nvidia.com

# --- Core Libraries ---
aiohappyeyeballs==2.6.1
aiohttp==3.12.14
aiosignal==1.4.0
annotated-types==0.7.0
anyio==4.9.0
attrs==25.3.0
certifi==2025.6.15
charset-normalizer==3.4.2
click==8.2.1
# Downgraded cloudpathlib to be compatible with weasel (a spacy dependency)
cloudpathlib==0.16.0
colorama==0.4.6
filelock==3.18.0
frozenlist==1.7.0
fsspec==2025.3.0
h11==0.16.0
huggingface-hub==0.33.1
idna==3.10
Jinja2==3.1.6
joblib==1.5.1
MarkupSafe==3.0.2
multidict==6.6.3
# Downgraded numpy to resolve core conflict between spacy/thinc and cuml/numba
numpy==1.26.4
packaging==25.0
# Downgraded pandas to resolve conflict with cuML
pandas==2.2.2
pillow==11.0.0
platformdirs==4.3.8
psutil==7.0.0
# Downgraded pyarrow to resolve conflict with cuML
pyarrow==19.0.0
pydantic==2.11.7
pydantic_core==2.33.2
python-dateutil==2.9.0.post0
pytz==2025.2
PyYAML==6.0.2
requests==2.32.4
six==1.17.0
# Downgraded smart-open to be compatible with spacy 3.7.4
smart-open==6.4.0
sniffio==1.3.1
tqdm==4.67.1
typing_extensions==4.14.0
tzdata==2025.2
urllib3==2.5.0
wrapt==1.17.2
xxhash==3.5.0
yarl==1.20.1

# --- Machine Learning & NLP ---
# Downgraded blis to be compatible with thinc 8.2.3
blis==0.7.11
catalogue==2.0.10
confection==0.1.5
cuml-cu12==25.4.0
cymem==2.0.11
datasets==4.0.0
dill==0.3.8
langcodes==3.5.0
marisa-trie==1.2.1
murmurhash==1.0.13
preshed==3.0.10
regex==2024.11.6
safetensors==0.5.3
# Pinned scikit-learn to the version used for training to prevent model inconsistency
scikit-learn==1.6.1
scipy==1.16.0
# Downgraded spacy to be compatible with numpy 1.x
spacy==3.7.4
spacy-legacy==3.0.12
spacy-loggers==1.0.5
srsly==2.5.1
# Downgraded thinc to be compatible with numpy 1.x
thinc==8.2.3
threadpoolctl==3.6.0
tokenizers==0.21.2
# The specific CUDA version build (+cu128) is removed as the index URL now handles it.
torch==2.7.1
torchaudio==2.7.1
torchvision==0.22.1
transformers==4.52.4
wasabi==1.1.3
# Removed hash check to bypass persistent installation error
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl


# --- API & Server ---
fastapi==0.115.13
httptools==0.6.4
mdurl==0.1.2
starlette==0.46.2
uvicorn==0.34.3
watchfiles==1.1.0
websockets==15.0.1

# --- Project Specific ---
xaif==0.3.6

# --- Development Tools (Optional but good practice) ---
accelerate==1.9.0
black==25.1.0
mypy_extensions==1.1.0
pathspec==0.12.1
Pygments==2.19.2
rich==14.0.0
shellingham==1.5.4
# Downgraded typer to be compatible with weasel (a spacy dependency)
typer==0.7.0
